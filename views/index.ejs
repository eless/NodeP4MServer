<% if (process.env.NODE_ENV == 'development') { %>
<script src="http://localhost:3000/socket.io/socket.io.js"></script>
<% } else { %>
<script src="https://p4m.herokuapp.com/socket.io/socket.io.js"></script>
<% } %>
<script>
    io = io.connect();
    var emitEvent = function(event){
        io.emit(event);
    }
</script>
<% include header %>
<body>
<% include navbar %>
<% if (!user) { %>
<h2>Welcome! Please log in.</h2>
<% } else { %>
<h2>Hello,
    <a href="/account"><img src=<%= user.photos && user.photos.length? user.photos[0].value: "" %> /><%= user.displayName %>.</a>
</h2>

<% } %>
<% include tournaments %>
</body>
<% include footer %>